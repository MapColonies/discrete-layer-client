/* eslint-disable @typescript-eslint/naming-convention */
export enum Type {
  required = 'required',
  pattern = 'pattern',
  min = 'min',
  max = 'max',
  minLength = 'minLength',
  maxLength = 'maxLength',
}
